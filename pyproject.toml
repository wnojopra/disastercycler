# pyproject.toml

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "disastercycler_engine"
version = "0.1.0"

[tool.setuptools]
packages = ["engine", "app"]

[tool.isort]
profile = "black"
